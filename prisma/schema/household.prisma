model Household {
    id String    @id @default(cuid())
    name         String    @default("Shared Household")
    members      User[]    @relation("HouseholdMembers")
    items        Item[]
    account      Account[]
}
